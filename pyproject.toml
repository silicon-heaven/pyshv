[project]
name = "pyshv"
version = "0.1.1"
description = "Pure Python SHV implementation"
readme = "README.rst"
authors = [
  { name="Elektroline a.s." },
]
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
]
requires-python = ">=3.10"
dependencies = [
  "python-dateutil",
]

[project.urls]
"Homepage" = "https://gitlab.com/elektroline-predator/pyshv"
"Bug Tracker" = "https://gitlab.com/elektroline-predator/pyshv/-/issues"
"Github" = "https://github.com/silicon-heaven/libshv-py"

[project.optional-dependencies]
docs = [
  "sphinx",
  "sphinx_rtd_theme",
  "sphinx-multiversion",
  "myst-parser",
]
test = [
  "pytest",
  "pytest-asyncio",
  "pytest-cov",
]
lint = [
  "black",
  "isort",
  "pydocstyle",
  "pylint",
  "mypy",
  "types-python-dateutil",
]

[project.scripts]
pycp2cp = "shv.cp2cp.__main__:main"
pyshvbroker = "shv.broker.__main__:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
exclude = ["docs", "tests*"]
[tool.setuptools.package-data]
"*" = ["py.typed"]

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.coverage.run]
branch = true
omit = ["tests/*"]
[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if typing.TYPE_CHECKING:"
]

[tool.isort]
profile = 'black'

[tool.pylint.main]
recursive = "y"
ignore = ["tests", "docs"]
fail-under = 9
[tool.pylint.format]
# C0103,C0116,C0115,C0114: We have pydocstyle for this.
# C0104: prevent usage of module name in documentation (seems unreasonable)
disable = "C0104,C0103,C0116,C0115,C0114"
# https://black.readthedocs.io/en/stable/guides/using_black_with_other_tools.html
max-line-length = 88
